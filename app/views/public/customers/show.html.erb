    <% if flash[:notice] %>
      <div class="alert alert-primary" role="alert"><strong><%= notice %></strong></div>
    <% end %>
    <% if flash[:alert] %>
      <div class="alert alert-danger" role="alert"><strong><%= alert %></strong></div>
    <% end %>


<div class="container-fluid">
	<div class="row">
		<div class="col-md-12">
			<h3>マイページ</h3>
			<p class="badge badge-default">登録情報</p>
			<%= link_to '編集する', customers_information_edit_path, class: 'btn btn-success' %>
			<table class="table table-bordered">
				<tbody>
					<tr>
						<td>氏名</td>
						<td><%= @customer.last_name %><%= @customer.first_name %></td>
					</tr>
					<tr>
						<td>カナ</td>
						<td><%= @customer.last_name_kana %><%= @customer.first_name_kana %></td>
					</tr>
					<tr>
						<td>郵便番号</td>
						<td><%= @customer.postal_code %></td>
					</tr>
					<tr>
						<td>住所</td>
						<td><%= @customer.address %></td>
					</tr>
					<tr>
						<td>電話番号</td>
						<td><%= @customer.telephone_number %></td>
					</tr>
	      	<tr>
						<td>メールアドレス</td>
						<td><%= @customer.email %></td>
					</tr>
				</tbody>
			</table>

    <div>
			<span class="badge badge-default">配送先</span>
			<%= link_to '一覧を見る', addresses_path, class: "btn btn-primary" %>
		</div>
		<div>
			<span class="badge badge-default">注文履歴</span>
			<button type="button" class="btn btn-primary">一覧を見る</button>
    </div>
		</div>
	</div>
</div>

