<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-6">
<%= form_with model: @genre, url: admin_genre_path(@genre.id) do |f| %>

<div class="field">
  <p>ジャンル編集</p>
    <div class="field"><%= f.text_area :name %></div>
<%= f.submit '変更を保存', class: 'btn btn-primary' %>
<% end %>

      </div>
    </div>
  </div>
</section>