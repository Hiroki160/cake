<section class="hero is-success">
  <div class="hero-body">
    <div class="container">
      <h3 class="title">
        注文履歴一覧<br><br>
      </h3>
    </div>
  </div>
</section>

<table class="table">
	<thead>
		<tr class="table-active">
			<th>購入日時</th>
			<th>購入者</th>
			<th>注文個数</th>
		</tr>
	</thead>

		<body>
	  <% @cart_items.each do |cart_item| %>
	  <tr>
	    <td><%= cart_item.created_at %></td>
	    <td><%= cart_item.customer.last_name %><%= cart_item.customer.first_name %></td>
	    <td><%= cart_item.amount %></td>
	    <td></td>
	  </tr>
	  <% end %>
	  <%= paginate @cart_items %>
	</body>