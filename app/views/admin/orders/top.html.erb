<section class="hero is-success">
  <div class="hero-body">
    <div class="container">
      <h3 class="title">
        注文履歴一覧<br><br>
      </h3>
    </div>
  </div>
</section>

<table class="table">
	<thead>
		<tr class="table-active">
			<th>購入日時</th>
			<th>購入者</th>
			<th>注文個数</th>
		</tr>
	</thead>

		<body>
	  <% @order_details.each do |o| %>
	  <tr>
	    <td><%= o.created_at %></td>
	    <td><%= o.order.name %></td>
	    <td><%= o.amount %></td>
	    <td></td>
	  </tr>
	  <% end %>
	</body>